v3.5.0

#---
# Apache & Python configuration
#---


# edit the file  c:/ms4w/Apache/conf/httpd.conf
(Look for the 'mod_wsgi' section and make sure that the settings below are set)
(verify that the correct file versions are used, e.g., '...cp38...' or '...cp39...')

	# mod_wsgi settings
	LoadFile "C:/ms4w/python/python39.dll"
	LoadModule wsgi_module "C:/ms4w/python/lib/site-packages/mod_wsgi/server/mod_wsgi.cp39-win32.pyd"
	WSGIPythonHome "C:/ms4w/python"
	SetEnv PYTHONHOME "C:/ms4w/python"

# copy favicon.ico to C:\ms4w\Apache\htdocs

# change your location to the 'pip' folder to install the required python libraries
$ c:
$ cd C:\ms4w\Python\Scripts

# Install additional python modules
$ pip install pathlib
$ pip install psycopg2

